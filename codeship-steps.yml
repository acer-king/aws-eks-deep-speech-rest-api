- type: manual
  name: unit_tests
  steps:
  - service: vote
    name: vote_tests
    command: echo "Tests passed!"

- type: parallel
  name: push_images
  tag: main
  encrypted_dockercfg_path: dockercfg.enc
  steps:
    - type: push
      service: vote
      name: push_vote_image
      image_name: 1991412/dastan
      image_tag: "{{.CommitID}}"
