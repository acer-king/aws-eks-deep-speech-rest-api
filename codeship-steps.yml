- type: parallel
  name: push_images
  tag: main
  encrypted_dockercfg_path: dockercfg.encrypted
  steps:
    - type: push
      service: vote
      name: push_vote_image
      image_name: dastan
      image_tag: "{{.CommitID}}"
